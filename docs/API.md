# API

## GET /health
Returns service status.

## POST /api/decide
Input: cart/customer behavioral signals. Output: explainable bounded action plus deterministic simulated outcome.

## GET /api/audit
Returns recent event → decision → outcome records.
